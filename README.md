# worldcup

An "ideal-type World Cup" knockout tournament picker — pick the winner of each head-to-head matchup until a single champion remains. Self-contained single-page web app.

## Usage

Open `index.html` in any modern browser — no build step or server required.
